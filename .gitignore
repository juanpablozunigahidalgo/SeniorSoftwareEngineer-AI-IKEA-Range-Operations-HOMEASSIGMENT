# Dependencias (Se instalan con npm install, no se suben)
node_modules/

# Build de producción (Se genera con npm run build)
# dist/

# Variables de entorno (¡CRÍTICO! Nunca subas tus contraseñas)
.env

# Logs de errores
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos del sistema operativo (Basura de Mac/Windows)
.DS_Store
Thumbs.db

# Carpeta de cobertura de tests (si agregamos tests después)
coverage/